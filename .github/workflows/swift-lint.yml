name: Swift Lint

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
